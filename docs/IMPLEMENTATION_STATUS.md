# My Love — статус реализации

Главная страница упрощена до двух UX-блоков: календарь и быстрые действия в одной строке на широком экране, с адаптивным расположением на узких экранах. Остальные dashboard-виджеты удалены с главной; календарь использует события и задачи текущего видимого периода. Верхний хедер раздела сохранён отдельной панелью. `AnimatedPanel` по умолчанию занимает доступные ширину и высоту через `w-full h-full`. Календарный блок главной переведён на `min-h-0 flex-1`, чтобы сжиматься по высоте без вертикального overflow страницы. Проверки: format:check, typecheck, lint, build, git diff --check — PASS; browser QA выполняет пользователь.

Ripple `Button` переработан без внешнего absolute-layer: волны снова являются дочерними элементами самой кнопки, имеют `inset-0` и не масштабируются за её границы. Убрана причина смещения border и растягивания по wrapper. Проверки: format, format:check, lint, typecheck, build, `git diff --check` — PASS; browser QA выполняет пользователь.

Ripple ограничен собственным wave-layer через `overflow-hidden`; scale волн уменьшен до 1.08 максимум, чтобы border не выходил выше/ниже кнопки. Проверки: format, format:check, lint, typecheck, build, `git diff --check` — PASS; browser QA выполняет пользователь.

Ripple `Button` получил жёсткие размеры wave-layer из `getBoundingClientRect()` кнопки в момент клика. Размер больше не берётся из растянутого wrapper; масштабирование волн сохраняется, но исходная геометрия всегда равна кнопке. Проверки: format, format:check, lint, typecheck, build, `git diff --check` — PASS; browser QA выполняет пользователь.

Ripple `Button` возвращён во внешний wave-layer; вместо переноса внутрь кнопки target-wrapper зафиксирован через `h-fit self-start`, чтобы сохранить волны и исключить растягивание border по высоте родителя. Проверки: format, format:check, lint, typecheck, build, `git diff --check` — PASS; browser QA выполняет пользователь.

Ripple `Button` закреплён внутри `motion.button`: wave-layer теперь получает размеры самой кнопки, а не flex-wrapper родителя. Particle burst оставлен без изменений. Проверки: format, format:check, lint, typecheck, build, `git diff --check` — PASS; browser QA выполняет пользователь.

`DatePicker` также выровнен по стандартной высоте control-компонентов: 40px с сохранением календарной кнопки и режимов `date`/`datetime-local`.

## 21 августа 2026 — shared control height

Высота стандартного `Button` (`size="m"`, `h-10`) синхронизирована с `Input`, `Textarea` и `Select`: все control-компоненты стартуют с высоты 40px. Для `Textarea` сохранено вертикальное изменение размера, но минимальная/начальная высота теперь 40px. Изменены только высота и вертикальные отступы, остальные размеры и поведение не тронуты. Проверки: format, format:check, lint, typecheck, build, `git diff --check` — PASS; browser QA выполняет пользователь.

## 21 августа 2026 — media audio/video contract sync

Проверены обновлённые media-контракты backend: `IMAGE`, `VIDEO`, `AUDIO`, legacy upload, direct S3 multipart (`initiate`, part upload, `complete`, `status`, `abort`), family stream/download, list/detail/delete. Frontend album теперь принимает image/video/audio, использует direct multipart upload с cleanup через abort, показывает audio/video preview и запускает аудио в глобальном MediaPlayer. Добавлены typed feature endpoints для upload-session flow; generated API не содержит новые операции, поскольку доступные `docs-json` (container и remote) старые, а текущий backend checkout не собирается из-за несвязанных users compile errors. Требуется обновить/redeploy backend runtime и повторить `npm run api:generate` из frontend.

Проверки frontend: `format`, `format:check`, `lint`, `typecheck`, `build`, `git diff --check` — PASS. Browser QA выполняет пользователь.

Последнее обновление: 20 августа 2026 года.

Добавлен общий `shared/ui/DatePicker` в стилистике `Input`: одинаковые размеры, focus/error-состояния, project color tokens и иконка календаря с открытием native picker. DatePicker подключён к регистрации, первой встрече и семейным событиям; нативные `input type="date"` в frontend-коде больше не используются. Проверки: typecheck, lint, build и `git diff --check`.

DatePicker расширен пропсом `withTime`: поля дедлайна задач, запуска регулярных задач, даты финансовой операции и второго напоминания семейного события теперь используют единый компонент с `datetime-local`; обычные календарные даты продолжают использовать режим даты.

Финансовый MVP расширен: добавлены создание кошельков, дата и категории доходов/расходов, переводы между кошельками, reversal операций, история и виджет статистики расходов по участникам. Финансовый раздел разделён на табы «Кошельки», «Новая операция» и «История операций» через общий `shared/ui/Tabs`. Исправлено создание операций: добавлена видимая валидация суммы/даты/перевода, исключён устаревший categoryId при переключении типа операции, после успеха очищаются поля и обновляется история. Подключён новый backend-контракт `GET /api/v1/families/me/finance/expense-statistics` с периодом и именами участников. Финансовые select-поля переведены на `shared/ui/Select`, добавлено создание категорий через `POST /api/v1/families/me/financial-categories`.

Устранён горизонтальный overflow в защищённом layout: основной scroll-контейнер явно ограничен по ширине и прокручивается только по вертикали, а семейный layout получил `min-w-0` для корректного сжатия контента рядом с Sidebar.

Исправлено выравнивание свёрнутого Sidebar: ссылки и callback-пункты используют одинаковое центрирование и горизонтальные отступы, поэтому активная иконка больше не получает визуально другую ширину.

Auth-маршруты (`/login`, `/auth`, `/restore`, `/reset-password`) объединены под гостевым `GuestRouteLayout` с общим `AppBackground` и приветственным изображением. Auth-панели получили ограниченную комфортную ширину `max-w-4xl` вместо растягивания на весь экран.

Фоновое изображение оптимизировано: PNG заменён на JPEG-ресурс для приложения без изменения визуального сценария приветственного экрана. Размер production-ресурса уменьшен примерно с 2.7 МБ до 649 КБ.

Проведён дополнительный KISS/DRY-аудит: базовые интервалы приведены к заявленным 5px, повторяющееся форматирование дат вынесено в `shared/lib/date`, Telegram polling не выполняется в фоне вкладки, а срок действия link-token показывается по фактическому `expiresAt`. Typecheck, lint, build и `git diff --check` проходят.

В семейный календарь добавлена настройка напоминаний событий: первое напоминание за заданное число минут, второй момент напоминания и выбор участников-получателей. Новые поля проходят валидацию, отправляются при создании/редактировании и показываются в деталях события.

Добавлена вкладка «Темизация» в `/settings`: пользователь может менять основные цветовые токены через native color picker, выбирать фоновое изображение до 5 МБ и сбрасывать оформление. Настройки применяются через CSS custom properties, сохраняются в `localStorage` и восстанавливаются до первого рендера приложения; фоновые изображения синхронизируются с `AppBackground`. Названия цветов в UI обезличены как «Атрибут-1» … «Атрибут-12», а изображение теперь выводится отдельным полноэкранным слоем, чтобы его не перекрывали фоновые градиенты.
Исправлено подключение `AppBackground` для защищённого основного маршрута: теперь экран настроек и все страницы приложения действительно находятся внутри фонового слоя, поэтому выбранное изображение применяется сразу и после перехода между маршрутами.
Устранено перекрытие фонового изображения `MainLayout`: непрозрачный фон основного контейнера заменён на полупрозрачный с лёгким blur, поэтому пользовательский фон виден на всех защищённых страницах без потери читаемости контента.
Стандартное изображение `main-bg.png` теперь относится только к приветственному маршруту `/`. После сброса темы во всём остальном приложении остаётся токенизированный цвет `--color-background`; пользовательское изображение продолжает отображаться на всех маршрутах, пока не будет удалено.
Обновлён общий `.page-header`: вместо непрозрачной чёрной плашки используется полупрозрачный glass-слой с мягким blur и тонкой границей. Это сохраняет закрепление заголовков и делает их визуально частью фоновой сцены на всех страницах.
Хедер приведён к базовой панели проекта: добавлены `--spacing-page`, токенизированные `--color-border`, общий `--radius-panel` и мягкая тень. Благодаря этому он визуально совпадает с остальными блоками без локальных значений отступов и радиусов.
Боковая навигация `my_family` приведена к той же панели: общий фон поверхности, border, радиус `--radius-panel`, базовый padding `--spacing-page`, blur и тень. Активные и hover-состояния теперь используют те же границы и поверхности, что и остальные блоки приложения.

Последний UI-срез: маршруты уведомлений и личного кабинета объединены в `/settings` с табами без смены URL; добавлен переиспользуемый `shared/ui/Tabs`.

Отступ `pb-24`, ранее добавлявшийся на страницах под нижний dock, удалён: прокрутка теперь не содержит искусственного нижнего пространства.

Нижнее меню теперь можно масштабировать по высоте: справа добавлен вертикальный доступный ползунок, работают также стрелки клавиатуры, диапазон ограничен 44–180 px, выбранный размер сохраняется в localStorage. Размер иконок, hover magnification и подъём иконок масштабируются вместе с dock в стиле macOS.

Для маршрутов раздела «Моя семья» добавлен переиспользуемый `PageLayout` в `shared/ui`: он задаёт единый scroll-контейнер, ширину и базовый межблочный отступ. В `@theme` добавлены токены `--spacing-block` (5px), `--text-body` (18px) и `--text-heading` (24px).

Добавлены токены `--spacing-gap` и `--spacing-page`: базовый gap теперь 5px, padding авторизованного page-контейнера — 10px через `MainLayout`.

Все классы `gap-*` в TSX-контейнерах заменены на единый `gap-gap` (5px), включая адаптивные `gap-x/gap-y`, чтобы межэлементные интервалы задавались одним токеном.

Ограничения `max-w-*` удалены из страниц, route-layout и общих контейнеров: основные экраны теперь растягиваются на всю доступную ширину.

Выпадающий список `Select` перенесён в `document.body` через React Portal. Позиция меню вычисляется относительно триггера и обновляется при resize/scroll, поэтому список отображается поверх карточек, overflow-контейнеров и stacking contexts.

Рендер портального списка упрощён: он больше не зависит от `AnimatePresence`-обёртки, которая могла препятствовать отображению portal-узла. Меню монтируется напрямую при открытии.

На странице задач форма создания/редактирования и список задач размещены в двух колонках 60/40. В элементах списка название, срок и исполнитель выстроены в одну строку с компактной адаптивной сеткой.

Структурные CSS-селекторы для sticky-заголовков удалены. Route-level заголовки используют явный класс `.page-header`, что исключает зависимость от вложенности DOM и упрощает поддержку новых страниц.

Проведён первый код-аудит по frontend best practices: убрана небезопасная цепочка `as unknown as` в календаре задач, добавлен runtime type guard для неизвестного task response, исправлены дублирующиеся `gap-gap`, а перегруженная разметка Sidebar отформатирована для читаемости. `generated/api.ts` не изменялся.

Нормализация неизвестного ответа задач вынесена из виджета календаря в `entities/task` (`getCalendarTasks` и `CalendarTask`), чтобы API-shape parsing не смешивался с UI-логикой и мог переиспользоваться другими потребителями.

В ходе продолжения аудита исправлены оставшиеся дробные интервалы `gap-2.5`, которые ранее были ошибочно затронуты механической заменой базовых gap-классов; базовый `gap-gap` применяется только к одноуровневым контейнерам.

Исправлен scroll на `/my_family/calendar` для узких экранов: страница календаря снова использует собственный `overflow-auto`, контентный контейнер имеет `min-h-full`, а внутренний widget больше не ограничивается `h-full` и устаревшим нижним padding.

Базовый `Button` теперь сам задаёт `inline-flex`, `items-center`, `justify-center` и `leading-none`. Иконки и текст в кнопках выровнены по общей вертикальной оси, поэтому локальные обёртки кнопок больше не дают визуального смещения иконок вверх.

Этот файл — точка входа для следующей рабочей сессии. Сначала прочитать его и `AGENTS.md`; обращаться к `PRODUCT_ROADMAP.md` только за подробным продуктовым контекстом. После каждого законченного вертикального среза обновлять чек-листы и журнал ниже.

## Текущий фокус

- Этап roadmap: **2 — Семейный стол и бытовой MVP**.
- Последний завершённый продуктовый срез: **назначение исполнителей задач и task gap-analysis**.
- Текущий срез: **lifecycle/visibility-аудит и недостающие UI primitives**.
- Следующий рекомендуемый срез: **authenticated visual QA lifecycle/Telegram и visibility/consent consumer после появления backend domain-модуля**.

## Уже было в проекте

- [x] React/TypeScript/Vite, FSD-структура и cyberpunk theme tokens.
- [x] Регистрация, вход, выход, JWT-сессия и закрытые маршруты.
- [x] Поиск пользователей и отправка приглашения в семью.
- [x] Backend приглашений: incoming/outgoing/accept/reject/cancel.
- [x] Backend семейных событий и сгенерированные RTK Query hooks.
- [x] Backend CRUD первой встречи и frontend-виджет чтения.
- [x] Базовые UI-компоненты: button, input, select, table, pagination, calendar, panels/layout.

## Прогресс roadmap

### Этап 0. Контракты и фундамент

- [ ] Роли `PARTNER`/`CHILD`, максимум два партнёра и жизненный цикл семьи.
- [x] Общая membership/role policy на backend.
- [ ] Visibility/consent model для чувствительных данных.
- [x] Единые API errors, money/date conventions и базовые idempotency rules.
- [ ] Notification/outbox contract и scheduled jobs.
- [ ] Недостающие UI primitives; backend e2e-каркас готов.

### Этап 1. Закрыть начатые сценарии

- [x] UI входящих/исходящих приглашений и действий с ними.
- [x] Закрытое приглашение партнёра по email/одноразовой ссылке.
- [x] Полный frontend CRUD первой встречи.
- [x] Рабочий календарь и управление family events.
- [x] Настоящее восстановление пароля через одноразовую ссылку.
- [x] Редактирование профиля через `GET/PATCH /users/me`.
- [x] Смена пароля и управление активными сессиями.
- [x] Dashboard MainPage на поддержанных backend-контрактах.
- [x] Базовый общий `AsyncState` для loading/error/empty и retry.
- [x] Security actions: request/confirm email change и request account deletion.

### Этапы 2–7

- [ ] Этап 2: dashboard, задачи, покупки и уведомления.
  - [x] Разовые задачи и dashboard-виджет задач.
  - [x] Регулярные задачи DAILY/WEEKLY.
  - [x] Назначение разовой и регулярной задачи участнику семьи.
  - [x] Отображение задач со сроком в семейном календаре.
  - [x] Списки покупок.
  - [x] In-app notifications и preferences.
  - [x] Dashboard «сегодня / неделя / нужно решение» и quick add.
- [ ] Этап 3: финансовый MVP.
- [ ] Этап 4: wellbeing и гармония.
- [ ] Этап 5: дети, питание и семейные рутины.
- [ ] Этап 6: воспоминания и долгосрочная ценность.
- [ ] Этап 7: production readiness.

## Принятые решения

- Семья целевой модели: два взрослых партнёра и их дети; расширенные родственники — только календарные записи/контакты.
- Wellbeing private-by-default: никаких диагнозов, скрытого анализа партнёра, рейтингов или слежения.
- API-контракт проверяется на backend до frontend-реализации; generated RTK Query code вручную не редактируется.
- Для frontend-задач backend read-only: агент только сверяет контракты и останавливает срез при недостающем backend API.
- Основной рекомендуемый family onboarding — закрытое приглашение знакомого партнёра; публичный каталог пока остаётся существующей функцией и будет пересмотрен отдельным срезом.
- Текущий закрытый onboarding использует передаваемую партнёру одноразовую ссылку, привязанную к точному email; автоматическая email-доставка появится после outbox/provider-контракта.
- После каждого frontend-среза выполняется браузерная проверка затронутых маршрутов согласно `AGENTS.md`.

## Известные пробелы

- Backend-сверка 16.08.2026: текущие стабильные финансовые frontend-контракты ограничены wallet API (`POST/GET/PATCH/DELETE /families/me/wallets`). В backend-документации заявлен следующий срез idempotent ledger-команд, однако в фактическом `src/modules/finance` пока присутствуют только wallet controller/service/DTO; endpoints income/expense/transfer и response DTO ещё не опубликованы. Финансовый UI поэтому не начинаем до появления и проверки этих контрактов. Следующим безопасным срезом остаётся lifecycle/visibility audit и UI primitives, не требующий новых backend endpoint.
- Для family lifecycle добавлена панель подтверждённых backend-команд: выход из семьи, архивация и восстановление. Она использует существующие generated RTK Query hooks, показывает ошибки API и явно объясняет сохранение shared data; команды dissolution и visibility-grants не выведены до отдельного UX-аудита и появления потребителя чувствительных данных.

- `MainPage` уже содержит первую встречу, семейные задачи и календарь, но пока не собран в целевой cockpit «сегодня / неделя / нужно решение».
- Family events пока не поддерживают recurrence, reminders, all-day и участников; текущий календарь работает с одиночными событиями и максимум 100 записями на видимый диапазон.
- Автоматических frontend-тестов пока нет.
- Shopping endpoints опубликованы без response DTO в Swagger, поэтому generated RTK Query возвращает `unknown`; frontend временно повторяет backend response DTO в `entities/shopping`.
- Notification inbox опубликован без response DTO, а preferences отсутствуют в текущем generated API; frontend временно отражает фактические backend DTO в `entities/notification` и подключает существующие preferences endpoints через RTK Query injection.
- Расширенный task MVP заблокирован отсутствующими backend-моделями и API для checklist items, postpone/reschedule с причиной, effort/mental-load, ротации исполнителей и недельной субъективной оценки справедливости. `assignedToId` уже поддерживается и подключён на frontend; «свободная задача» представлена `null`.
- В браузерной проверке гостевых маршрутов обнаружен отсутствующий `/favicon.ico` (404); на функциональность страниц не влияет.
- Адаптивность панели безопасности исправлена: внутренние формы больше не переполняют узкий экран, а подтверждение email складывается в колонку на мобильной ширине. В меню для раскрытых разделов добавлено видимое название родительского блока.
- Исправлено обрезание hover-shadow у панелей главной страницы: scroll-колонки получили внутренний отступ, чтобы внешнее свечение не упиралось в границы overflow-контейнера.
- Блоки страницы `my_family` переведены на общий `shared/ui/AnimatedPanel`, включая карточки участников и состояние отсутствующей семьи; дублирующие стили контейнеров удалены.
- Проведён аудит повторяющихся page/feature-контейнеров: профиль, безопасность, family activity, invitations и private invitations переведены на `AnimatedPanel`; специализированные calendar/table/inner-state блоки оставлены без замены из-за собственного overflow/layout.
- Маршруты переведены на route-level code splitting через `React.lazy`/`Suspense`; общий fallback загрузки вынесен в `shared/ui/route-loading`. Стартовый JS entry уменьшен с 855 КБ до 205 КБ (gzip с 264 до 66 КБ), страницы собираются отдельными чанками. Дополнительные дубли UI-контейнеров не выносились без подтверждённой повторяемости, чтобы не создавать преждевременные абстракции.
- Дополнительно лениво загружаются `RouteTransition`, `MainRouteLayout` и `AppBackground`: общий UI-чанк уменьшен с 351 КБ до 125 КБ, React/animation зависимости вынесены в отдельные кешируемые чанки. Поведение гостевых и защищённых маршрутов сохранено.

## Журнал сессий

| Дата       | Срез                                     | Результат                                                                                                                                                                                                                                                                                                                                                            | Проверки                                                                                                                                                                                        | Следующий шаг                                                                                                                 |
| ---------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-15 | Аудит и roadmap                          | Созданы `PRODUCT_ROADMAP.md`, правила безопасности и этапы 0–7                                                                                                                                                                                                                                                                                                       | `git diff --check`                                                                                                                                                                              | Начать первый вертикальный срез                                                                                               |
| 2026-08-15 | Приглашения                              | Добавлены `/family-invitations`, incoming/outgoing states, accept/reject/cancel, навигация и корректная invalidation family/users cache; API error parser вынесен в `shared`                                                                                                                                                                                         | `typecheck`, `lint`, `build`; route `200`, API без токена `401`                                                                                                                                 | Реализовать frontend CRUD первой встречи                                                                                      |
| 2026-08-15 | Первая встреча                           | Добавлены create/edit/delete, Zod-валидация, подтверждение удаления, понятная ошибка прав и общий `Textarea`; nullable description обработан без правки generated API                                                                                                                                                                                                | `typecheck`, `lint`, `build`, `format:check`                                                                                                                                                    | Реализовать календарь и family events                                                                                         |
| 2026-08-15 | Backend foundation                       | Добавлены роли/lifecycle семьи, единая membership policy, DB-лимит двух партнёров и изолированный security e2e harness                                                                                                                                                                                                                                               | backend `lint`, unit, e2e, `build`                                                                                                                                                              | Унифицировать базовые API-контракты                                                                                           |
| 2026-08-15 | Backend API contracts                    | Без изменения текущих форм добавлены error codes/details/requestId, общий offset pagination и date/time/money conventions                                                                                                                                                                                                                                            | backend `lint`, unit, e2e, `build`                                                                                                                                                              | Cursor pagination, concurrency и idempotency отдельными срезами                                                               |
| 2026-08-15 | Семейный календарь                       | Добавлены `/family-calendar`, загрузка событий по видимому диапазону, agenda дня, create/edit/confirm/reject/delete, timezone семьи, optimistic `If-Match`, навигация и адаптация `/families/me` к `403`; OpenAPI nullable-типы и codegen из `.env` синхронизированы                                                                                                 | frontend `format:check`, `typecheck`, `lint`, `build`; backend `format:check`, `lint`, 24 unit, e2e, `build`; миграции применены                                                                | Реализовать закрытое приглашение партнёра                                                                                     |
| 2026-08-15 | Backend concurrency                      | В ответы first-date/events добавлен `version`, PATCH поддерживает optional `If-Match`; stale update возвращает `VERSION_CONFLICT` без изменения старых frontend-запросов                                                                                                                                                                                             | backend `prisma:generate`, `lint`, unit, e2e, `build`, production Docker build                                                                                                                  | Реализовать транзакционную idempotency для критических команд                                                                 |
| 2026-08-15 | Backend idempotency и roadmap sync       | Optional `Idempotency-Key` резервирует payload hash и переигрывает сохранённый ответ; этапы backend A–H перенумерованы ровно в frontend 0–7                                                                                                                                                                                                                          | backend `prisma:generate`, `lint`, unit, e2e, `build`; сверка обоих roadmap                                                                                                                     | Завершить foundation этапа 0, затем общий этап 1                                                                              |
| 2026-08-15 | Закрытые приглашения                     | Backend хранит только hash email-bound токена и поддерживает expiry/cooldown/revoke/accept после регистрации; frontend создаёт, копирует и отзывает ссылки, `/join-family` сохраняется через login/register; старый `recipientId` flow сохранён; глобальный scrollbar сделан тоньше в theme colors                                                                   | frontend `format:check`, `typecheck`, `lint`, `build`; backend `prisma:generate`, `format:check`, `lint`, 24 unit, 3 e2e, `build`, production Docker build; миграция `20260815030000` применена | Реализовать frontend профиля                                                                                                  |
| 2026-08-15 | Backend-профиль                          | Добавлены совместимые `GET/PATCH /users/me`, locale, timezone, `version` и optional `If-Match`; email и дата рождения намеренно не меняются общей командой                                                                                                                                                                                                           | backend `prisma:generate`, `lint`, 24 unit, 3 e2e, `build`; миграция на чистой БД                                                                                                               | Смена пароля и управление сессиями                                                                                            |
| 2026-08-15 | Frontend-профиль                         | `ProfilePage` заменена на загрузку и форму редактирования имени, фамилии, пола, телефона, locale, timezone и описания; данные Redux синхронизируются после сохранения, `If-Match` обрабатывает конфликт версий; email и дата рождения отображаются как read-only                                                                                                     | frontend `format:check`, `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                   | Запросить backend-контракт для смены пароля и сессий                                                                          |
| 2026-08-15 | Пароль и сессии                          | В `ProfilePage` добавлены смена пароля, список активных сессий с текущим устройством, отзыв одной не-текущей или всех остальных сессий; контракт сверялся с опубликованным Swagger, backend не изменялся                                                                                                                                                             | frontend `api:generate`, `format:check`, `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                   | Запросить backend-контракт для восстановления пароля                                                                          |
| 2026-08-15 | Восстановление пароля                    | Legacy `/auth/restore` заменён на generated `POST /auth/password-reset/request`; добавлен `/reset-password?token=...` с подтверждением нового пароля, валидацией токена и понятными success/error states                                                                                                                                                             | frontend `api:generate`, `format:check`, `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                   | Согласовать общие states и заполнить MainPage                                                                                 |
| 2026-08-15 | Dashboard MainPage                       | Убрана заглушка главной страницы; добавлены карточки трекера первой встречи, семейного календаря и переход в календарь. Не добавлялись задачи/покупки/уведомления без соответствующих backend endpoints                                                                                                                                                              | frontend `format:check`, `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                   | Согласовать общие loading/empty/error/optimistic states                                                                       |
| 2026-08-15 | Общие async states                       | Добавлен переиспользуемый `shared/ui/async-state` для loading/error/empty/retry; профиль переведён на единый error/loading flow. Backend не требовался: срез UI-инфраструктурный                                                                                                                                                                                     | frontend `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                                   | Подключить state-компонент к календарю, приглашениям и остальным спискам                                                      |
| 2026-08-15 | States приглашений                       | Incoming/outgoing списки семейных приглашений переведены на общий `AsyncState`; сохранены отдельные empty-сообщения и retry. Backend не изменялся                                                                                                                                                                                                                    | frontend `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                                   | Подключить state-компонент к календарю и остальным спискам                                                                    |
| 2026-08-15 | States семейного календаря               | Загрузка и ошибка настроек семьи в `FamilyCalendar` переведены на общий `AsyncState`; специальное состояние отсутствия семьи и ошибки событий сохранены                                                                                                                                                                                                              | frontend `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                                   | Подключить state-компонент к остальным спискам и мутациям                                                                     |
| 2026-08-15 | Security actions                         | После регенерации Swagger добавлены формы запроса/подтверждения смены email и запроса удаления аккаунта; backend исходники также содержат family lifecycle/export/audit, но эти пути не опубликованы в текущем `docs-json`, поэтому они не обходились ручными запросами                                                                                              | frontend `api:generate`, `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                   | Опубликовать актуальный Swagger для family lifecycle/export/audit, затем реализовать эти действия                             |
| 2026-08-15 | States страницы семьи                    | Загрузка и ошибка `MyFamilyPage` переведены на общий `AsyncState`; сценарий отсутствия активной семьи сохранён отдельно                                                                                                                                                                                                                                              | frontend `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка не выполнялась                                                                                                   | Подключить state-компонент к остальным спискам и мутациям                                                                     |
| 2026-08-15 | Tasks page                               | Добавлен защищённый маршрут `/tasks` и пункт «Задачи» в меню семьи: создание/редактирование с описанием, сроком и приоритетом, фильтры, завершение/возврат и архивирование через generated RTK Query API                                                                                                                                                             | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Следующий срез: task-routines                                                                                                 |
| 2026-08-15 | Кнопки и задачи в календаре              | Общий `Button` теперь выравнивает иконку и текст в одну строку; задачи с `dueAt` загружаются через generated RTK Query и отображаются как элементы семейного календаря, клик ведёт на `/tasks`                                                                                                                                                                       | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: защищённый `/family-calendar` корректно перенаправлен гостя на `/`                                                                | Следующий срез: task-routines                                                                                                 |
| 2026-08-15 | Регулярные задачи                        | Добавлен защищённый `/task-routines`: создание правил DAILY/WEEKLY с интервалом, приоритетом и следующим запуском, ручная генерация задачи и архивирование через generated RTK Query; добавлен пункт меню                                                                                                                                                            | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: защищённый маршрут корректно перенаправлен гостя на `/`                                                                           | Следующий срез: shopping lists                                                                                                |
| 2026-08-16 | Списки покупок                           | Добавлен защищённый `/shopping-lists` и пункт «Покупки» в меню семьи: создание и архивирование списков, добавление товаров с количеством, отметка и возврат товара; loading/error/empty/retry переведены на `AsyncState`. Из-за `unknown` response в Swagger backend DTO временно отражены в `entities/shopping`                                                     | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: редирект гостя, create list, add item, check/uncheck, desktop и 390px                                                             | Следующий срез: in-app notifications и preferences                                                                            |
| 2026-08-16 | Уведомления                              | Добавлен защищённый `/notifications`, inbox с read/read-all, настройки in-app/email/Telegram/quiet hours и счётчик непрочитанных в dock. Preferences подключены через RTK Query injection по существующему backend-контракту, которого пока нет в generated API                                                                                                      | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: mark read, quiet hours, сохранение preferences, desktop и 390px                                                                   | Следующий срез: dashboard «сегодня / неделя / нужно решение»                                                                  |
| 2026-08-16 | Dashboard cockpit                        | `MainPage` перестроена в ежедневный cockpit: «Сегодня» с просрочками, «На неделе», «Нужно решение» из приглашений/event proposals/notifications, счётчик inbox, quick add-переходы к задачам, событиям и покупкам; сохранён компактный блок истории семьи                                                                                                            | `typecheck`, `lint`, `build`, `git diff --check`; Playwright на наполненных API states: секции cockpit, quick action, desktop и 390px                                                           | Следующий срез: gap-analysis расширенного task MVP                                                                            |
| 2026-08-16 | Task assignees и gap-analysis            | В разовые и регулярные задачи добавлен выбор исполнителя из `families/me`, явный вариант свободной задачи и отображение назначения. Backend подтверждает membership validation и перенос assignee из routine в generated task. Остальные расширения task MVP зафиксированы как отсутствующие backend contracts                                                       | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: assignee options и payload для task/routine, desktop и 390px                                                                      | Следующий срез: foundation/privacy audit                                                                                      |
| 2026-08-16 | Оптимизация загрузки и FSD-аудит         | Все page-модули загружаются по маршруту через `React.lazy`/`Suspense`; добавлен общий accessible fallback в `shared/ui/route-loading`. Повторяющиеся контейнеры проверены: существующие общие блоки уже используют `AnimatedPanel`/`AsyncState`, новые абстракции без устойчивого повтора не добавлялись                                                             | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: `/`, `/login`, `/main` (lazy fallback и protected redirect)                                                                       | Следующий срез: foundation/privacy audit                                                                                      |
| 2026-08-16 | Оптимизация общих зависимостей           | Лениво подключены `RouteTransition`, `MainRouteLayout` и `AppBackground`; общий UI-чанк уменьшен до 125 КБ, React и animation runtime получили отдельные кешируемые чанки.                                                                                                                                                                                           | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: гостевой `/`, `/login`, редирект `/main`                                                                                          | Следующий срез: foundation/privacy audit                                                                                      |
| 2026-08-16 | Backend contract audit                   | Сверены backend status/plan и фактический код: lifecycle, audit, calendar projection, wallets, tasks, shopping и notifications доступны; заявленный ledger-срез пока не имеет фактических income/expense/transfer endpoints и DTO. Финансовый frontend до публикации контракта заблокирован, foundation/privacy audit можно продолжать.                              | Read-only сверка backend controllers, DTO, privacy policy и Prisma schema; `git diff --check`                                                                                                   | Реализовать foundation/privacy audit без новых API; затем повторно проверить ledger contract                                  |
| 2026-08-16 | Family lifecycle foundation              | В `MyFamilyPage` добавлена FSD-feature `family-lifecycle`: безопасные действия выхода, архивации и восстановления через существующие backend endpoints, подтверждение destructive действий, disabled/loading/error states и сохранение shared data в UX-тексте.                                                                                                      | `typecheck`, `lint`, `build`, `git diff --check`; Playwright: protected `/my_family` корректно редиректит гостя на `/`                                                                          | Следующий срез: authenticated browser verification lifecycle; затем visibility/consent consumer после появления domain-модуля |
| 2026-08-16 | Главная и боковое меню                   | Добавлена сворачиваемая левая навигация `shared/ui/sidebar` для desktop с сохранением мобильного нижнего dock; cockpit главной получил более близкое к целевому эскизу двухколоночное расположение виджетов и адаптивные ширины.                                                                                                                                     | `typecheck`, `lint`, `build`, `git diff --check`; визуальная проверка desktop/mobile требует authenticated browser session                                                                      | Следующий срез: authenticated visual QA главной и lifecycle controls                                                          |
| 2026-08-16 | Меню только в «Моя семья»                | Левая навигация убрана из общего `MainLayout`; теперь она отображается только внутри `MyFamilyPage` и содержит ровно маршруты бывшего `children` раздела «Моя семья»: календарь, задачи, регулярные задачи, покупки и приглашения.                                                                                                                                   | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Проверить авторизованный вид страницы семьи и переходы из бокового меню                                                       |
| 2026-08-16 | Family section layout                    | Меню перенесено в отдельный pathless `FamilySectionLayout`; все маршруты семейного меню теперь отрисовываются через общий layout, поэтому Sidebar не исчезает при переходе между календарём, задачами, покупками и приглашениями.                                                                                                                                    | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Проверить авторизованные переходы между всеми family routes                                                                   |
| 2026-08-16 | Вложенный family routing                 | `my_family` стал родительским маршрутом с дочерними `/my_family/calendar`, `/my_family/tasks`, `/my_family/task-routines`, `/my_family/shopping-lists`, `/my_family/family-invitations`; добавлен пункт «Общие», состояние Sidebar сохраняется, а RouteTransition отключён внутри family-раздела.                                                                    | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Выполнить authenticated browser QA семейного layout и переходов                                                               |
| 2026-08-16 | Исправление active state Sidebar         | Для верхних пунктов бокового меню включено точное сопоставление `NavLink end`, поэтому «Общие» больше не остаётся активным на дочерних family-маршрутах.                                                                                                                                                                                                             | `git diff --check`                                                                                                                                                                              | Выполнить authenticated browser QA active states                                                                              |
| 2026-08-16 | Telegram connection flow                 | В уведомлениях добавлен FSD-блок подключения Telegram: link-token, deep link к боту, одноразовый fallback-код с копированием и TTL-подсказкой, polling/focus refresh статуса, отключение с подтверждением и связь с существующим `telegramEnabled`. Backend read-only; контракты сверены с `telegram.controller`/DTO.                                                | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Выполнить authenticated manual QA: connect, return/refresh, expired token, disconnect, preferences                            |
| 2026-08-16 | Логотип и favicon                        | Логотип размещён в `public/assets/my-love-logo.png` и используется как favicon сайта, в dock-пункте «Моя семья», пункте «Общие» и слева от кнопки сворачивания Sidebar.                                                                                                                                                                                              | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Выполнить authenticated visual QA семейного меню                                                                              |
| 2026-08-16 | Sticky header family layout              | Для outlet-контейнера семейного layout добавлены `h-full`, `min-h-0` и `overflow-hidden`, чтобы внутренний `PageLayout` оставался единственным scroll-контейнером и `.page-header` корректно фиксировался при прокрутке содержимого.                                                                                                                                 | `typecheck`, `lint`, `git diff --check`                                                                                                                                                         | Проверить sticky header на `/my_family` и дочерних family-маршрутах                                                           |
| 2026-08-16 | Единый scroll-контейнер страниц          | Убрана конкурирующая вертикальная прокрутка из `MainLayout`; промежуточные layout-контейнеры больше не создают скрытый scroll-контекст. Теперь прокрутка выполняется самим содержимым страницы, поэтому sticky-заголовки family-раздела фиксируются корректно.                                                                                                       | `typecheck`, `lint`, `git diff --check`                                                                                                                                                         | Проверить прокрутку и sticky header на авторизованных маршрутах                                                               |
| 2026-08-16 | Равные колонки семейного календаря       | Блок добавления/редактирования события и календарь переведены на симметричную двухколоночную сетку с одинаковой шириной на широких экранах.                                                                                                                                                                                                                          | `typecheck`, `git diff --check`                                                                                                                                                                 | Выполнить визуальную проверку `/my_family/calendar`                                                                           |
| 2026-08-16 | Единый каркас прокрутки семейных страниц | `PageLayout` разделяет фиксированный каркас страницы и внутренний `overflow-auto` контейнер. Заголовки `.page-header` остаются прижатыми к верхней границе, а прокручивается только содержимое маршрутов `my_family`.                                                                                                                                                | `typecheck`, `lint`, `git diff --check`                                                                                                                                                         | Выполнить визуальную проверку всех маршрутов `my_family`                                                                      |
| 2026-08-16 | Sticky header календаря                  | Заголовок `FamilyCalendar` подключён к общему `.page-header`, поэтому на `/my_family/calendar` он остаётся закреплённым над прокручиваемым календарём и формой события.                                                                                                                                                                                              | `typecheck`, `lint`, `build`, `git diff --check`                                                                                                                                                | Выполнить визуальную проверку календаря при прокрутке                                                                         |
| 2026-08-20 | Lifecycle confirm primitive              | Добавлен accessible `shared/ui/ConfirmDialog` через portal с Escape/backdrop cancel, focus на отмене и loading-состоянием; выход/архивация семьи и отключение Telegram переведены с `window.confirm` на единый диалог. Visibility/consent UI не добавлялся: backend domain-контракт и потребитель чувствительных данных отсутствуют.                                 | `npm run typecheck`, `npm run build`, `git diff --check`; authenticated browser QA не выполнен в текущей сессии                                                                                 | Следующий срез: authenticated visual QA lifecycle/Telegram; затем visibility/consent consumer после backend-контракта         |
| 2026-08-20 | Backend contract gap closure             | По фактическим Nest controllers/DTO добавлены typed RTK Query hooks для всего `wellbeing` (check-ins, consent, assessments, trends/export, gratitude, support requests, rituals, couple meetings), `families/me/children` и task reminders. Runtime Swagger на `:5000` оказался неполным и не использовался как источник для перезаписи существующего generated API. | `npm run typecheck`, `npm run build`, `git diff --check`; backend source/DTO read-only audit                                                                                                    | Следующий срез: privacy-aware wellbeing UI и child profiles после согласования экранов                                        |

# 2026-08-20 visual slice: wellbeing route/panel, child profile CRUD panel, task reminder controls. Checks: typecheck/build/diff-check PASS. Browser QA pending authenticated session.

# 2026-08-20 audit slice: WellbeingPanel decomposed into CheckInForm/CheckInList; checkbox label linked for accessibility. Checks: lint/typecheck/diff-check PASS. Authenticated browser QA pending.

# 2026-08-20 list scroll slice: Notifications page now uses a full-height layout with overflow isolated to the incoming notifications list; mobile keeps page-level overflow for settings content. Checks: format:check/typecheck/diff-check PASS. Browser QA pending.

# 2026-08-20 backend contract sync: Runtime Swagger was stale/incomplete and would remove existing generated operations, so generated API was preserved. Added confirmed finance archive/restore/list-archived hooks for wallets, financial goals, and recurring payments from backend controllers. Checks: format:check/typecheck/diff-check PASS. Browser QA pending.

# 2026-08-20 backend Swagger refresh: `/docs-json` теперь содержит полный контракт; generated RTK Query API обновлён. Адаптированы изменившиеся operation IDs/DTO-типы для shopping, tasks и family events. Уведомления переведены на растягиваемую desktop-панель с внутренним scroll списка и отдельным scroll настроек. Checks: format:check/typecheck/diff-check PASS. Browser QA pending.

# 2026-08-20 family privacy slice: Добавлен маршрут `/my_family/children` и пункт «Дети» в левое меню family-раздела; wellbeing получил явный consent-блок с выдачей и отзывом доступа к scopes. Существующие child CRUD и wellbeing check-in hooks переиспользованы. Checks: typecheck/format:check/diff-check PASS. Browser QA pending.

# 2026-08-20 notifications bugfix: `NotificationsPage` переключена с ошибочного `useList3Query` (кошельки) на `useList11Query` (`GET /api/v1/notifications`), поэтому заголовок и текст снова берутся из уведомлений. Checks: typecheck/format:check/diff-check PASS. Следующий срез: authenticated visual QA; browser blocked by `missing field sandboxPolicy`.

# 2026-08-20 child routines: в `/tasks` и `/task-routines` добавлены выбор ребёнка при создании, фильтрация списков и отображение привязанного профиля; payload передаёт `childId` в подтверждённый backend DTO. Checks: typecheck/format:check/diff-check PASS. Browser QA не выполнялась по запросу пользователя.

# 2026-08-20 notifications scroll fix: у desktop-панели inbox зафиксирована flex-высота, а список получил `h-0 flex-1 overflow-y-auto`; длинные уведомления больше не растягивают страницу и прокручиваются внутри блока. Checks: typecheck/format:check/diff-check PASS. Browser QA не выполнялась по запросу пользователя.

# 2026-08-20 notification badge sync: `MainRouteLayout` исправлен с ошибочного `useList3Query` (wallets) на `useList11Query` (notifications), поэтому badge непрочитанных использует тот же источник, что и inbox. Checks: typecheck/format:check/diff-check PASS.

# 2026-08-20 theme/panel slice: в настройках темизации добавлена сохраняемая настройка прозрачности `AnimatedPanel` (20–100%); `FirstDateTracker` и его состояния переведены на общий `AnimatedPanel`; правило оборачивать самостоятельные визуальные блоки в `AnimatedPanel` добавлено в `AGENTS.md`. Checks: format:check/typecheck/diff-check PASS; browser QA не выполнялась по запросу пользователя.

# 2026-08-20 button click effect: общий `Button` получил `clickEffect` (по умолчанию `true`); при клике через portal разлетаются разноцветные вращающиеся сердечки, эффект отключается через `clickEffect={false}`. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA pending.

# 2026-08-20 button click effect refinement: сохранены исходные волновые кольца кнопки; сердечки теперь сначала разлетаются радиально во все стороны, затем падают вниз. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA pending.

# 2026-08-20 button theme color: добавлен сохраняемый токен `--color-button` в темизацию; цвет рамки/свечения кнопок, волн и сердечек теперь настраивается единым значением, сердечки больше не разноцветные. Checks: pending.

# 2026-08-20 panel blur setting: в темизацию добавлена сохраняемая настройка размытия `AnimatedPanel` (0–32 px), применяемая через CSS custom property. Checks: format:check/typecheck/diff-check PASS; browser QA не выполнялась по запросу пользователя.

# 2026-08-20 collapsed sidebar sizing: в свёрнутом Sidebar зафиксированы минимальная ширина контейнера, `min-w-0` у layout-обёрток и `shrink-0` у логотипа/иконок, чтобы элементы не сжимались во время transition и в конечном collapsed state. Checks: format/format:check/typecheck/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 sidebar icon sizing follow-up: для верхней кнопки, пунктов меню, дочерних пунктов и выхода заданы явные `h/w` и `shrink-0`, чтобы SVG не сужались при закрытии Sidebar. Checks: format:check/typecheck/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 collapsed sidebar alignment: исправлена collapsed-разметка Sidebar: логотип скрывается в закрытом состоянии, кнопка переключения центрирует только стрелку, а ссылки меню занимают всю ширину и центрируют иконки. Checks: format/format:check/typecheck/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 collapsed active item shape: активный пункт свёрнутого Sidebar переведён в фиксированный квадрат 56×56 с `rounded-full`, чтобы фиолетовая рамка и фон были круглыми, а иконка оставалась по центру. Checks: format/format:check/typecheck/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 collapsed sidebar gutter fix: устранено переполнение круглого активного пункта из-за одновременных padding у `AnimatedPanel` и `nav`; в collapsed-состоянии применяются компактные `!p-2`/`!p-1`, поэтому круг полностью помещается и центрируется внутри Sidebar. Checks: format/format:check/typecheck/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 deploy build fix: CI падал на шаге `npm run lint` из-за двух несвязанных с Sidebar label-ошибок в `ThemeSettingsPanel`: range inputs не имели `id`, а labels — `htmlFor`. Связи добавлены; полный workflow-порядок `format:check`, `typecheck`, `lint`, `build`, `git diff --check` проходит. Сервер `185.227.144.160` read-only проверен: контейнер `my-love-frontend` запущен, текущий HTTP healthcheck отвечает.

# 2026-08-20 page headers: все 13 найденных обёрток с классом `page-header` заменены на `AnimatedPanel` с сохранением класса для sticky-позиционирования и существующей layout-логики. Checks: format/format:check/typecheck/build/diff-check PASS; lint заблокирован двумя существующими jsx-a11y ошибками в `ThemeSettingsPanel.tsx` (строки 100 и 120); authenticated browser QA не выполнялась.

# 2026-08-20 my-family header surface: удалено дублирующее визуальное оформление `.page-header` (фон, blur, border и shadow); sticky-хедеры теперь используют единый визуальный слой `AnimatedPanel`. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 panel shadow clipping: layout-обёртки основной области и family-раздела переведены с `overflow-hidden` на `overflow-visible`; внутренний scroll уже контролируется страницами, поэтому тени `AnimatedPanel` больше не срезаются по краям контейнеров. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 main idle cinematic mode: на главной странице крупные блоки после 5 секунд бездействия плавно уходят вниз с последовательным stagger/fade, оставляя фон; движение курсора, фокус, клик или scroll запускают красивое возвращение блоков. Реализовано через `motion` с очисткой таймера при unmount. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 main panel collapse refinement: idle-анимация главной усилена эффектом плохо закреплённых панелей: блоки падают с чередующимся левым/правым transform-origin, rotate/rotateY/skew, scale, смещением и blur. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 main house-collapse staging: обзорные панели разделены на отдельные «элементы конструкции»; при бездействии они обрушиваются сверху вниз с чередующимися направлениями и отдельными задержками, вместо исчезновения единым блоком. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA не выполнялась.

# 2026-08-20 main collapse scrollbar state: во время обрушения главной добавлен локальный класс `main-idle-scroll`, скрывающий scrollbar только у scroll-контейнера страницы; при активности класс снимается, и scrollbar возвращается. Checks: format/format:check/typecheck/lint/build/diff-check PASS; browser QA не выполнялась.

# 2026-08-21 media album: added `/my_family/media` with sidebar navigation, generated Swagger media contracts, multipart `file` upload, paginated table, filename/date filters, private detail preview via `GET /media/:id`, and S3/metadata deletion via `DELETE /media/:id`. Backend source and Swagger were audited read-only; generated API was refreshed. Checks: `format`, `format:check`, `typecheck`, `git diff --check`; authenticated browser QA pending.

# 2026-08-21 reusable modal: added shared `Modal` with React Portal, outside-click/Escape close handling, focus entry, and animated open/close transitions. Media preview and `ConfirmDialog` now use the shared modal shell. Added the modal usage rule to `AGENTS.md`. Checks: format/typecheck/lint/build/diff-check; browser QA blocked by missing `sandboxPolicy`.

# 2026-08-21 modal close bugfix: fixed cached media detail data keeping the preview open after close by binding preview visibility to active `selectedId`; added pointer cursors for modal backdrop and close control. Checks: format:check/lint/typecheck/build/diff-check.

# 2026-08-21 performance slice: throttled main dashboard idle reset, coalesced magnetic button and footer pointer work to animation frames, and disabled background glitch layers under reduced-motion preference. Static checks: format:check/lint/typecheck/build/diff-check.

# 2026-08-21 local media player slice: добавлен persistent `MediaPlayer` с mini/full состояниями, drag-перемещением поверх маршрутов, сохранением положения/громкости/видимости в localStorage и базовым Web Audio EQ. Добавлен защищённый `/media` с вкладками плейлиста и настроек, добавлением URL/локального аудиофайла и таблицей из shared UI; backend не используется. Browser QA pending.

# 2026-08-21 media player bugfix: `MediaPlayerProvider` и overlay вынесены выше keyed `RouteTransition`, поэтому player не размонтируется при переходах между маршрутами. Timeline теперь не перезагружает source при обычном rerender, seek отделён локальным scrub-state и вычисляет позицию по координате pointer, а `timeupdate` coalesced через requestAnimationFrame. Mini-состояние оставляет только название и три action-кнопки. Checks: format:check/lint/typecheck/build/diff-check PASS; browser QA выполняется пользователем.

# 2026-08-21 timeline click correction: добавлен финальный `click`-расчёт позиции по `clientX` поверх native range, чтобы обычный клик не откатывался controlled value к началу трека. Checks: format:check/lint/typecheck/build/diff-check PASS; browser QA выполняется пользователем.

# 2026-08-21 timeline root fix: native `input[type=range]` полностью заменён на custom accessible `role=slider` с единым pointer capture lifecycle и keyboard seek. Это убирает конфликт browser native value с React controlled state, который возвращал позицию к началу. Checks: format:check/lint/typecheck/build/diff-check PASS; browser QA выполняется пользователем.
